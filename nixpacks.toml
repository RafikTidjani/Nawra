# Nixpacks configuration
[variables]
NODE_VERSION = "20"

[phases.install]
cmds = ["cd nawra-project && npm ci"]

[phases.build]
cmds = ["cd nawra-project && npm run build"]

[start]
cmd = "cd nawra-project && npm run start"
